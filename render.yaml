services:
  - type: web
    name: bioinfo-hub
    runtime: static
    buildCommand: npm run docs:build
    staticPublishPath: docs/.vitepress/dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
